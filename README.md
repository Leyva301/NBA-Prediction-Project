# NBA-Prediction-Project

# Set up:

1. Create virtual environment: "python3 -m venv venv"
2. Connect to virtual environment: "source <name of virtual environment>/bin/activate" 
3. Install dependencies: "pip install requirements.txt"
4. Run the model: "python nba_prediction_model.py"